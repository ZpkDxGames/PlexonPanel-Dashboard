import { test } from "node:test";import assert from "node:assert/strict";import React from "react";import { renderToStaticMarkup } from "react-dom/server";
import { emptyControlState } from "../.test-dist/lib/control-state.js";
import { ConsoleView,ChatView,PluginsView,OverviewView } from "../.test-dist/app/control-views.js";
import { FilesView,BackupsView,ServerView } from "../.test-dist/app/advanced-views.js";
const props={state:emptyControlState("test"),can:()=>false,run:async()=>{throw new Error("denied");},notice:()=>{},connected:false};
test("read-only console and chat omit execution controls",()=>{const console=renderToStaticMarkup(React.createElement(ConsoleView,props)),chat=renderToStaticMarkup(React.createElement(ChatView,props));assert.ok(!console.includes('aria-label="Console command"'));assert.ok(!chat.includes('type="submit"'));assert.match(console,/Read-only console/);});
test("host and file routes show explicit unavailable states without privileged controls",()=>{for(const view of [FilesView,BackupsView,ServerView]){const html=renderToStaticMarkup(React.createElement(view,props));assert.ok(!html.includes('>Restore</button>'));assert.ok(!html.includes('>Restart</button>'));assert.ok(!html.includes('>Save</button>'));assert.ok(/unavailable|not installed|companion/i.test(html));}});
test("uploaded-looking log content and plugin names remain escaped text",()=>{const state={...props.state,plugins:[{name:'<img src=x onerror=alert(1)>',version:"1",authors:[],enabled:true}],console:[{content:'<script>alert(1)</script>',level:"WARN"}]};for(const View of [PluginsView,OverviewView]){const html=renderToStaticMarkup(React.createElement(View,{...props,state}));assert.ok(!html.includes('<script>alert(1)</script>'));assert.ok(!html.includes('<img src=x'));}});
