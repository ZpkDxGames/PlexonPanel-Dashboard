rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Protocol v2 never grants the browser direct Firestore access. The
      // Cloud Run gateway uses Admin SDK credentials and authorizes devices.
      allow read, write: if false;
    }
  }
}
